@model IEnumerable<HeThongDiemDanh.Models.DIEMDANHSV>
@{
    ViewBag.Title = "DanhSachDiemDanh";

    Layout = "~/Views/Shared/_LayoutGiangvien.cshtml";
}

<table id="example" class="table table-striped table-bordered nowrap" style="width:100%">
    <thead>
        <tr>
            <th>STT</th>
            <th>MSSV</th>
            <th>Họ tên</th>
            <th>1</th>
            <th>2</th>
            <th>3</th>
            <th>4</th>
            <th>5</th>
            <th>6</th>
            <th>7</th>
            <th>8</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td></td>
                <td>
                    @Html.Raw(item.IDNGUOIDUNG)

                </td>
                <td>@Html.Raw(item.IDNGUOIDUNG)</td>
                <td>

                    <input @(Convert.ToBoolean(item.lan1) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan2) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan3) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan4) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan5) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan6) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan7) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
                <td>
                    <input @(Convert.ToBoolean(item.lan8) == true ? "checked='checked'" : string.Empty) type="checkbox" />
                </td>
            </tr>
        }
    </tbody>
</table>